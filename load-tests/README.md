# Load Tests

This folder contains a simple `k6` script to smoke-test the application.

Run locally (install k6):

```bash
k6 run k6-script.js
```

Adjust `vus` and `duration` in the script to match your target load.
